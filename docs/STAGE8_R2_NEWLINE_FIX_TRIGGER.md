# Stage 8-R2 newline-fix trigger

This temporary no-data marker creates a pull-request synchronize event after the reviewed one-time workflow already exists on the branch. It contains no private corpus content and grants no acceptance, release, or freeze authority.
