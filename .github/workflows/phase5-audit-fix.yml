name: phase5-audit-fix

on:
  push:
    branches:
      - feature/hybrid-retrieval-v5.5-alpha1

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Apply semantic regeneration audit fix
        shell: bash
        run: |
          set -euo pipefail
          test -f .phase5-review-fix.py
          python .phase5-review-fix.py
          python -m pip install .
          python -m compileall -q tkr tests
          python -m unittest discover -s tests -v
      - name: Commit validated audit fix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tkr/hybrid_retrieval.py tests/test_hybrid_retrieval_adversarial.py
          git rm .phase5-review-fix.py
          git rm .github/workflows/phase5-audit-fix.yml
          git commit -m "fix(retrieval): re-run Phase 4 before indexing"
          git push origin HEAD:feature/hybrid-retrieval-v5.5-alpha1
